const AND = {
  symbol: "A",
  input: 2,
  output: 1,
  operator: '&&'
};

const OR = {
  symbol: "O",
  input: 2,
  output: 1,
  operator: '||'
};

const NOT = {
  symbol: "N",
  input: 1,
  output: 1,
  operator: '!'
};

module.exports = [AND, OR, NOT];
