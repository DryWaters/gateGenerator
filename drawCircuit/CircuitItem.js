export default class CircuitItem {
  constructor(x, y, height, width, value) {
    this.x = x;
    this.y = y;
    this.height = height;
    this.width = width;
    this.value = value;
  }
  draw(ctx) {}
}
